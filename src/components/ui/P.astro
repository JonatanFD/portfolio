---
interface Props {
    text?: string;
    className?: string;
}
const { text, className = "" } = Astro.props;
---

<p class:list={["leading-7 [&:not(:first-child)]:mt-6", className]}>
    {text}
    <slot />
</p>
